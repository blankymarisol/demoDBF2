<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns:fx="http://javafx.com/fxml/1"
            xmlns="http://javafx.com/javafx"
            fx:controller="Controller.LoginController"
            stylesheets="@theme.css"
            style="-fx-background-color: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">

    <center>
        <VBox alignment="CENTER" maxWidth="450" styleClass="card">
            <padding>
                <Insets top="40" right="40" bottom="40" left="40"/>
            </padding>

            <!-- Logo/Título -->
            <VBox alignment="CENTER" spacing="10">
                <Label text="🏢" style="-fx-font-size: 64px;"/>
                <Label text="DevSolutionsF" styleClass="titulo-principal"/>
                <Label text="Sistema de Gestión" style="-fx-font-size: 14px; -fx-text-fill: #7F8C8D;"/>
            </VBox>

            <Separator>
                <VBox.margin>
                    <Insets top="25" bottom="25"/>
                </VBox.margin>
            </Separator>

            <!-- Formulario -->
            <VBox spacing="20">

                <!-- Campo Usuario -->
                <VBox spacing="5">
                    <Label text="Usuario o Correo Electrónico" styleClass="label-form"/>
                    <TextField fx:id="txtUsuario"
                               promptText="correo@ejemplo.com"
                               prefHeight="45"/>
                </VBox>

                <!-- Campo Contraseña -->
                <VBox spacing="5">
                    <Label text="Contraseña" styleClass="label-form"/>
                    <PasswordField fx:id="txtContrasena"
                                   promptText="Ingrese su contraseña"
                                   prefHeight="45"/>
                    <TextField fx:id="txtPasswordVisible"
                               promptText="Ingrese su contraseña"
                               prefHeight="45"
                               visible="false"
                               managed="false"/>
                </VBox>

                <!-- Mostrar contraseña -->
                <CheckBox fx:id="chkMostrarPassword"
                          text="Mostrar contraseña"
                          style="-fx-font-size: 13px;"/>

                <!-- Mensaje -->
                <Label fx:id="lblMensaje"
                       wrapText="true"
                       style="-fx-min-height: 25px; -fx-font-weight: bold;"/>

                <!-- Botón Login -->
                <Button fx:id="btnLogin"
                        text="Iniciar Sesión"
                        onAction="#iniciarSesion"
                        styleClass="btn-primary"
                        prefHeight="45"
                        maxWidth="Infinity"
                        defaultButton="true"/>

            </VBox>

        </VBox>
    </center>

    <!-- Footer -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-padding: 20;">
            <Label text="© 2025 DevSolutionsF | v1.0.0"
                   style="-fx-text-fill: white; -fx-font-size: 12px;"/>
        </HBox>
    </bottom>

</BorderPane>